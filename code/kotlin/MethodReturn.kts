fun multiple(a: Int, b: Int): Int {
  return a * b
}
println(multiple(2, 3))

fun divide(a: Int, b: Int): Int {
  if (a == 0) return 0
  return a / b
}
println(divide(0, 10))

fun defaultValue() {
}
println(defaultValue())
